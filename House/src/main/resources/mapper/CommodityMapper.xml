<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.house.mapper.CommodityMapper">
    <select id="pageCommodity" resultType="com.project.house.entity.Commodity">
        select * from goods ${ew.customSqlSegment}
    </select>
    <select id="listCommodity" resultType="com.project.house.entity.Commodity">
        select * from goods
    </select>

</mapper>